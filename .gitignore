# Swift / SPM
.build/
*.o
*.d
*.swp

# Xcode
*.xcodeproj/
xcuserdata/
DerivedData/
*.xcworkspace/

# macOS
.DS_Store
**/.DS_Store

# Build output — everything in dist/ is generated (app, dmg, icons, appcast.json)
dist/
# Cloudflare wrangler local state/cache (includes account info — never commit)
.wrangler/
.claude/
docs/linkedin-mockup.html

# Local-only planning scratch file (references signing identity, not for upstream)
TODO.md
